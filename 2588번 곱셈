const fs = require('fs');
const inputDate = fs.readFileSync('dev/stdin').toString().split('\n');
const num1 = +inputDate[0];
const num2 = +inputDate[1];

const oneNum = num2 % 10;
const tenNum = Math.floor(num2%100/10);
const hanNum = Math.floor(num2/100);

console.log(num1 * oneNum);
console.log(num1 * tenNum);
console.log(num1 * hanNum);
console.log(num1 * num2);
