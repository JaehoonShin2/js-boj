const fs = require('fs');
const input = fs.readFileSync('dev/stdin').toString();
const a = parseInt(input);
let answer = ''

for ( i=1;i<=a; i++){
    answer += parseInt(i)+'\n'
};

console.log(answer)
