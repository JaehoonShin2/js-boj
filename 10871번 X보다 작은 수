const fs = require('fs')
const input = fs.readFileSync('dev/stdin').toString().split('\n');

let input0 = input[0].split(' ');
let N = Number(input0[0]);
let X = Number(input0[1]);
let input1 = [];
    input1 = input[1].split(' ');

let answer = ''

for ( i = 0; i < N; i++){
    if ( Number(input1[i]) < X ){
        answer += input1[i]+' ';
    };
};

console.log(answer)
