const { info } = require('console');
const fs = require('fs');
const inputDate = fs.readFileSync('dev/stdin')
.toString().split(' ');

const a = +inputDate[0];
const b = +inputDate[1];

if(a<b){
    console.log("<");
} else if(a>b){
    console.log(">");
} else{
    console.log("==");
};
